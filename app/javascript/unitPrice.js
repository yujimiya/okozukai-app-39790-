// document.addEventListener('DOMController', function() {
//   // 単亜を取得する
//   const unitPriceInput = document.getElementById('event_unit_price')
//   const unitPrice = parseFloat(unitPriceInput.value)
//   // 単価を設定する処理
//   eventController.setPrice(unitPrice)
// })

//   //フォームが送信された時の処理
// document.querySelector('.form').addEventListener('submit', function(event) {
//   const unitPriceInput = document.getElementById('event_unit_price')
//   ecentController.setPrice(parseFloat(unitPriceInput.value))

//   this.submit

//   const totalAmountElement = document.getElementById('totalAmount')
//   totalAmountElement.innerText = '合計金額： ' + eventController.calculateTotalAmount() 
// })

//必要ないかも（読み込む設定も削除する）